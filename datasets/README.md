# Datasets

Keep large datasets out of GitHub.

Recommended approach:

- Add dataset download code inside notebooks.
- Store temporary downloaded files in a local `data/` folder.
- Commit only small sample files when absolutely needed.
